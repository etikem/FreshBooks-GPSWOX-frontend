# Origin of the backend API server.
# Next.js rewrites /api/:path* → ${NEXT_PUBLIC_API_URL}/admin/:path*
# (see next.config.js). The backend mounts admin routes under /admin
# (see backend/src/index.ts). Keep these in sync if either side changes.
NEXT_PUBLIC_API_URL=http://localhost:4000
